/**
 * Resend client scaffold.
 *
 * The `resend` package is installed but intentionally not invoked yet.
 * TODO: enable when keys are added.
 *
 * import { Resend } from "resend";
 *
 * export const resend = new Resend(process.env.RESEND_API_KEY!);
 *
 * // Confirmation + order-notification emails are sent from the Stripe webhook
 * // handler once checkout.session.completed fires. See README.
 */

export {};
